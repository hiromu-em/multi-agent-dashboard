# 作業ログ

日付ごとに追記する。**新しい日付を上に積む（降順）**。実装の設計判断は AGENTS.md に、機能の説明は README.md にまとめてあるので、ここには「その日に何があったか」だけを書く。

## 2026-09-06

### 1. ボードの並び順の調整

- 同じ状態グループ（完了、エラー・停止済み）の中での並びを、終了時刻の降順から昇順へ変更（`4b58ce6`）。新しく終わったレーンがグループの右端に足されるだけになり、既にあるレーンを追い越して割り込まなくなった

### 2. 会話表示のMarkdown対応

- インラインMarkdown（`**太字**`・`` `コード` ``・`*斜体*`）を解釈するようにした（`05975ba`）。生のHTMLタグは意図的に非対応のまま（会話ログはエージェントの出力なので、そのまま描画させない）
- インラインコードの背景色が暗い盤面に同化して読みにくかったため、ステータス配色と重ならない紫系に変更（`e9f89ac`）
- 行頭の `- ` `* ` を `・` に変換（`0dd4b04`）。太字の `**` とは区別している

### 3. 「届かなかった指示」表示の修正

- 再送して成功した指示が、1時間のあいだ「送信失敗」としてヘッダーに残り続けていた（`701b316`）。同じ宛先へその後 `delivered` の記録があれば解消済みとみなして外すようにした

### 4. ヘッダーの折りたたみ

- ヘッダーを畳んでレーンの表示領域を広げるボタンを追加（`f1adea2`）。畳んだ状態でも細いバーに戻すボタンが残る

### 5. レーンへの返信欄の追加

- 「ダッシュボードに指示入力欄は置かない」という設計上の決定に対し、**対話待ちレーンへの返信に限った例外**として返信欄を追加（`a70ee65`）。エージェントの質問文に列挙があればボタン化し、無ければ自由入力欄だけになる
- 実際に試したところ、`state: "blocked"`（対話待ち）はCLIが数秒で `done` にしてしまうため、**返信欄が押す前に消える**ことが判明。条件を「実行中以外のすべて」に広げ、対話待ちのときだけ琥珀色で強調する形に変更（`4260141`）

### 6. 終わったレーンの自動消去を廃止

- 終了から3時間で盤面から落とす仕組み（`RETENTION_MS`）を廃止し、各レーンの「×」ボタンによる手動片付けに変更（`a7e16f4`）。「対話待ちは打ち切らない」という例外を設けてあったが、`blocked` がほとんど観測できないため実際には効いておらず、返事待ちのレーンが黙って消えていた

### 7. 窓口セッションからの不具合報告と対応

- 窓口で `#A ...` と打った指示が配送されず、`.logs/dispatch.jsonl` にも記録が残らない事象の報告を受領。原因は `routePrompt` が最初に呼ぶ `listAgents()` の失敗で、サーバーが `block: false` を返すため宛先付きの指示が窓口のClaudeへ素通りしていたこと。宛先の有無をCLIに依存せず判定し、宛先付きなら必ず止めて記録するよう修正（`372fce1`）
- 併せて、CLI呼び出しの失敗時に stdout / stderr も記録する変更を `worktree-gateway` から取り込み（`5eef626`）。この事象自体は、長時間動かした devサーバーの劣化が原因と窓口側で切り分け済み

### 8. `AskUserQuestion` まわりの調査と対応

- 対話的な選択肢提示ツール（`AskUserQuestion`）の呼び出しが、盤面では「▸ AskUserQuestion」とだけ表示され、質問文も選択肢も一切見えていなかった。質問と選択肢を読める発言として組み立て直すよう修正（`1787c46`）
- 検証の結果、**保留中のあいだ質問がJSONLに書き込まれるまで大きな遅延がある**ことが判明（30秒待っても現れず、後になって記録された）。対話待ちのレーンに限り `claude logs` から今の質問を補って表示するようにした（`f1c5f05`）。JSONL側に追いついたら補完はスキップする（`f0ef4f6`）
- 完了レーンの報告文に含まれるMarkdownの箇条書きが選択肢として誤検出され、答え終わった報告が「まだ選べる質問」に見えていた。ボタン化を対話待ちレーンだけに限定（`a735eaf`）
- 併せて、`AskUserQuestion` で止まっているレーンには**返信の1回目が届かない**（CLIが保留中のツール呼び出しへの素のテキストを「拒否・聞き直し要求」として扱う）ことも検証で確認。CLI側の仕様のためダッシュボードでは直せず、AGENTS.md に注意点として記録

### 9. 後片付け

- テスト用に立てたセッション（`テスト1`、`返信欄テスト`、`AskUserQuestion表示テスト`、`パース検証用`）をすべて削除

## 2026-09-05

### 1. ダッシュボード疎通確認

`#F` 宛てに疎通テストを送信。一度文字化けが発生したが、テストコマンド側の不具合と判明し、再送して疎通確認を完了。

### 2. 窓口セッションの運用に関するQ&A（`#A` とのやり取り）

- 窓口セッションの立ち上げ方（バックグラウンド起動 → sessionIdを窓口として登録 → `claude attach` で開く、という流れ）を確認
- 宛先の自動登録の仕組みについて質問・イメージ合わせ
- 自動化スクリプト `node scripts/new-gateway.mjs` を作成し、実際に `窓口2` を作成して動作確認
- `claude attach` 後にagents画面へ戻ると操作できなくなる件を調査し解決
- 役目を終えた `窓口2` の削除を依頼

### 3. 窓口ワークツリー（`worktree-gateway`）での作業

- README.md が AGENTS.md の実装内容（宛先スティッキー方式、`.logs/gateway.json` による窓口除外、`dispatch.ts`/`dispatch-log.ts`/`lane-registry.ts` など）に対して古くなっていたため、記載を実装に合わせて更新
- 更新差分を `worktree-gateway` ブランチでコミット（`3b4c4f0` "Sync README with implemented dispatch/lane-registry design"）
- コミットを main へ反映する方法を確認。リモートは未設定であり、main は別ワークツリー（`#A` が動いているディレクトリ）でチェックアウト中のため、窓口ワークツリー側からは直接マージできないと判明
- そのため `#A` へ `git merge worktree-gateway` の実行を依頼し、`0a9aa2b` として main に反映

### 4. 宛先解除後のステータスライン表示についての調査

- `#` 単独で宛先を解除した直後、ステータスラインに宛先表示が残っているように見える件を調査
- サーバー側（`/api/dispatch` のGET、`.logs/target.json`、`.logs/dispatch.jsonl` の `cleared` イベント）はいずれも正しく解除済みであることを確認
- 原因は恐らく、宛先解除の入力がフックでblockされモデルのターンが発生しないため、ステータスラインの再描画タイミングが少し遅れること（`refreshInterval` 未設定）。設定ファイル（`claude_project/.claude/settings.json`）のパスや内容自体に誤りは無いことも確認済み
